import SignIn from '@/components/auth/signin/SignIn';

export const metadata = {
    title: 'Sign In | Skillprobe',
    description: 'Sign in to access courses, mentors, and sessions.',
};

export default function SignInPage() {
    return <SignIn />;
}
